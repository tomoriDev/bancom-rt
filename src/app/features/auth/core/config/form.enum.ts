export enum EEmailErrorMessages {
  REQUIRED = 'El campo correo es obligatorio',
  INVALID = 'No es un email válido',
}

export enum EPasswordErrorMessages {
  REQUIRED = 'El campo contraseña es obligatorio',
}
